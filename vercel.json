{
  "buildCommand": "yarn build",
  "outputDirectory": "dist",
  "builds": [
    { 
      "src": "index.ts",
      "use": "@vercel/node"
    }
  ],
  "functions": {
    "api/*.ts": { "runtime": "nodejs22.x" }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/index" }
  ]
}