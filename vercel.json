{
  "buildCommand": "yarn build",
  "outputDirectory": "dist",
  "builds": [
    { 
      "src": "index.ts",
      "use": "@vercel/node"
    }
  ],
  "rewrites": [
    { "source": "/(.*)", "destination": "/index" }
  ]
}