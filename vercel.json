{
  "buildCommand": "yarn build",
  "outputDirectory": "dist",
  "builds": [
    { 
      "src": "index.ts",
      "use": "@vercel/node"
    }
  ],
  "functions": {
    "api/*.ts": { "maxDuration": 30 }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/index" }
  ]
}