{
  "buildCommand": "yarn build",
  "outputDirectory": "dist",
  "builds": [
    { 
      "src": "./api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}